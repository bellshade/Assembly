pull_request_rules:
  - name: Merge dengan label
    conditions:
      - "#approved-reviews-by>=1"
      - check-succes=assembly testing file - Pull Request
      - base=main
      - label=ready-to-merge
    actions:
      merge:
        method: squash